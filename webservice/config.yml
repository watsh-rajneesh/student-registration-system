katharsis:
  host:          http://localhost:8080
  searchPackage: edu.sjsu.cohort6.esp.service.rest

database:
  server: localhost
  port: 27017
  dbName: testreg

# use the simple server factory if you only want to run on a single port
#server:
#  type: simple
#  connector:
#    type: http
#    port: 8080

server:
  type: simple
  applicationContextPath: /
  adminContextPath: /admin
  connector:
    type: http
    port: 8080

logging:
  level: ALL

  appenders:
    - type: console